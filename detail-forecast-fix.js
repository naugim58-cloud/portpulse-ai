function addDetailForecast(){if(route!=='port-detail')return;const data=document.querySelector('.detail-screen-data');if(!data||data.querySelector('.detail-forecast'))return;const score=congestionScore(selectedPort);data.querySelector('.calc-box')?.insertAdjacentHTML('beforebegin',`<div class="detail-forecast"><div class="detail-forecast-head"><div><h3>혼잡도 현황 및 24시간 예측</h3><small>현재 기준 -1시간부터 +23시간</small></div><span>${congestionStatus(score)} · ${score}점</span></div>${homeForecastSvg(score,selectedPort+4)}</div>`)}
const detailForecastBase=render;render=function(){detailForecastBase();addDetailForecast()};render();
